import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agendaST-0223',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
